import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    headerContainer: {
        marginHorizontal: 30,
        paddingBottom: 50,
    },
    scrollContainer: {
        flexGrow: 1,
        marginHorizontal: 30,
    },
});